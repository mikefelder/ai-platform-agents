# Pinned runtime dependencies for the Lambda deployment package.
# Kept separate from pyproject.toml dev extras so `pip install -r` produces
# a minimal zip suitable for Lambda.
boto3>=1.34
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
azure-monitor-opentelemetry-exporter>=1.0.0b31
